class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        window = len(needle)
        for i in range(0, len(haystack)-window+2):
            if (needle==haystack[i:i+window]):
                return i
        return -1
