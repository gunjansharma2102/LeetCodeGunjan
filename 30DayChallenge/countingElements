class Solution:
    def countElements(self, arr: List[int]) -> int:
        count=0
        arr1=set(arr)
        for x in arr:
            if x+1 in arr1:
                count+=1
        return count
                
